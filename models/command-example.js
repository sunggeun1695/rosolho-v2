const { Message, Client } = require("discord.js");

module.exports = {
    name: "",
    aliases: [],
    description: "",
    category: "",
    example: [""],
    /**
     * 
     * @param {Client} client 
     * @param {Message} message 
     */

    run: async (client, message, args, prefix) => {
       // 코드
    }
}